//to find the length of strings

#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    char *string = "welcome";
    int i;

    for(i = 0; string[i] != '\0'; i++)
    {

    }

    printf("length is %d\n",i);
    return(0);
}